<% @users.each do |user| %>
  <h2><%= user.user_name %></h2>
  <p> Email: <%= user.email %></p>
  <br>
  <%= link_to "Click me", admin_user_path(user) %>
  <%= link_to "Edit me", edit_admin_user_path(user) %>
  <%= link_to "Delete User", admin_user_path(user), method: :delete, data: {Confirm: "Are you sure?"}  %>
  <hr>
<% end %>
