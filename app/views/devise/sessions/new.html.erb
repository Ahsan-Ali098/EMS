<div class="container-fluid w-25 border rounded shadow mt-5 p-4 text-center">
  <h2>Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field mb-3 text-start">
    <%= f.label :login %><br />
    <%= f.text_field :login, required: true, autofocus: true %>
  </div>

  <div class="field mb-3 text-start">
    <%= f.label :password %><br />
    <%= f.password_field :password, required: true, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field mb-3 text-start">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions mb-2">
    <%= f.submit "Log in", class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <button class="sign-up-button btn btn-dark"> Sign Up</button><br>
<% end %>
<%= render "devise/shared/links" %>

</div>

<div class="d-none sign-up-form">
  <%= render file: "devise/registrations/new" %>
</div>

