<h2>Cart show</h2>
<ul>
  <% @cart.order_items.each do |order_item| %>
    <li>Item: <%= order_item.product.title %></li>
    <li>Price: <%= number_to_currency(order_item.product.price) %></li>
    <li>Quantity: <%= order_item.quantity %></li>
    <li>Total Price: <%= number_to_currency(order_item.total_price) %></li>
      </li>
    # See lineItem section below for explanation of line_item links
    <ul>
      <li>
        <%= link_to "Add one", order_item_add_path(:id => order_item), method: :post %>
        <%= link_to "Reduce one", order_item_reduce_path(:id => order_item), method: :post %>
      </li>
      <li>
        <%= link_to "Remove item", user_order_item_path(order_item), method: :delete %>
      </li>
    </ul>
    #
  <% end %>
</ul>
<h3>Sub total: <%= number_to_currency(@cart.sub_total) %></h3>
# See Order section below for explanation of new_order_path
<%= link_to "Proceed to checkout", new_user_order_path %><br>
#
<%= link_to "Empty cart", user_carts_path(@current_cart),method: :delete, data: {confirm: "Are you sure?"}